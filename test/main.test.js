import { afterAll, beforeAll, describe, it, expect } from 'vitest';

beforeAll(async () => {});

afterAll(async () => {});

describe('main', () => {
    it('should work', async () => {
        expect(1).toBe(1);
    });
});
